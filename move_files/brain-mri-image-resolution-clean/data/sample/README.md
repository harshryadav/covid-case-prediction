# data/sample

Tiny synthetic phantom slices for smoke tests and CI. Generated by:

```
brainsr-preprocess --build-sample --output-dir data/sample
```

Files in this directory are committed only when small enough; the generator
above is fully reproducible (seeded), so deleting them and regenerating gives
the same set.

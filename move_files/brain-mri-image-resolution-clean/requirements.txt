# Pinned mirror of pyproject.toml deps for Docker reproducibility.
# Torch is installed by the base image, so we omit it here.
numpy>=1.26,<3
scipy>=1.11
h5py>=3.10
Pillow>=10.0
scikit-image>=0.22
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.8
torchmetrics>=1.3
tensorboard>=2.16

# Dev / test
pytest>=8.0
pytest-cov>=4.1
ruff>=0.4
